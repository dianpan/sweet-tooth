var Main = React.createClass({
  render: function(){
    // var shops = this.props.data.map(function(shop){
    //   return <Shop {...shop} />
    // });

    return (
    <div>
      hshs
    </div>
    )
  }
});
